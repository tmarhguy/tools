#!/usr/bin/env bash
# Wrapper — Hash generator

set -euo pipefail

ROOT="$(cd "$(dirname "$0")/.." && pwd)"
TOOL="$ROOT/tools/dev/hash-gen.sh"

if [[ ! -x "$TOOL" ]]; then
  echo "Error: hash-gen tool is not installed yet." >&2
  exit 1
fi

exec "$TOOL" "$@"
