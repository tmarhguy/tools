#!/usr/bin/env bash
# Wrapper — Video to GIF

set -euo pipefail

ROOT="$(cd "$(dirname "$0")/.." && pwd)"
TOOL="$ROOT/tools/video/to_gif.sh"

if [[ ! -x "$TOOL" ]]; then
  echo "Error: Video to GIF tool is not installed yet." >&2
  echo "Expected script: $TOOL" >&2
  exit 1
fi

exec "$TOOL" "$@"
